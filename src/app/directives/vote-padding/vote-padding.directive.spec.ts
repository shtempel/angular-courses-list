import { VotePaddingDirective } from './vote-padding.directive';

describe('VotePaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new VotePaddingDirective();
    expect(directive).toBeTruthy();
  });
});
