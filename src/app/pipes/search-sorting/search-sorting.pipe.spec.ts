import { SearchSortingPipe } from './search-sorting.pipe';

describe('SearchSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
